apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "lamp-app.fullname" . }}-test-connection"
  labels:
    {{- include "lamp-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "lamp-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
